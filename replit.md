# Desarrollo local en Replit

## Arranque

Este proyecto es una aplicación PHP sin dependencias externas. El workflow `Start application` ejecuta:

```bash
php -S 0.0.0.0:5000 -t .
```

El preview web de Replit utiliza el puerto 5000. La página principal se sirve desde `index.php`.